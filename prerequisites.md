# Prerequisites

- Azure subscription (free tier sufficient for most components)
- Azure CLI installed and authenticated (`az login`)
- PowerShell access to SOC VM via Azure Bastion
- Permissions: Contributor role at subscription scope
